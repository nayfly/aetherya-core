# Upstream provider credentials, read only by the gateway service.
# Kept out of .env so the decision service never receives a credential it
# has no use for. Without one the gateway starts, answers /health, and
# returns 502 on every completion.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
